import { Box } from "@mui/material";
import { APP_BG_LIGHT } from "../utils/colors";

const BecomeWorker = () => {
  return (
    <Box
      sx={{
        backgroundColor: APP_BG_LIGHT,
        paddingTop: "50px",
        paddingBottom: "20px",
      }}
    >
      <h1>This is BecomeWorker Page</h1>
      <h1>Coming Soon....</h1>
    </Box>
  );
};

export default BecomeWorker;
